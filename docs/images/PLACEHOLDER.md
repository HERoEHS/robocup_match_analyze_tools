# Screenshot Placeholders

Add the following screenshots to this directory so README images render correctly on GitHub.

| Filename | Content |
|----------|---------|
| `realtime_monitoring.png` | `robocup_realtime_monitoring` web UI — live field with robots, ball, and strategy zones |
| `match_analyzer.png` | `robocup_match_analyzer` web UI — replay field with heatmap and Voronoi overlay |
| `sim_2d.png` | `robocup_match_2d_simulation` web UI — 2D field with ally circles, opponent diamonds, and ball |
| `blackbox_web_download.png` | `robocup_blackbox` web download UI — session list with download buttons |

## Recommended capture steps

1. Launch each tool following its README.
2. Open the URL in a browser.
3. Take a screenshot (1280×720 or wider).
4. Save as PNG in this directory with the filename above.
